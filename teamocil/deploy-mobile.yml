windows:
  - name: "deploy mobile"
    root: "~"
    layout: tiled
    panes:
      - cmd: "ssh $( knife search 'hostname:mobile-13 AND chef_environment:production' | grep IP | sed 's/IP: *//' )"
      - cmd: "ssh $( knife search 'hostname:mobile-14 AND chef_environment:production' | grep IP | sed 's/IP: *//' )"
      - cmd: "ssh $( knife search 'hostname:mobile-15 AND chef_environment:production' | grep IP | sed 's/IP: *//' )"
      - cmd: "ssh $( knife search 'hostname:mobile-16 AND chef_environment:production' | grep IP | sed 's/IP: *//' )"
      - cmd: "ssh $( knife search 'hostname:mobile-17 AND chef_environment:production' | grep IP | sed 's/IP: *//' )"
      - cmd: "ssh $( knife search 'hostname:mobile-18 AND chef_environment:production' | grep IP | sed 's/IP: *//' )"
