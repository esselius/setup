windows:
  - name: "deploy image-service"
    root: "~"
    layout: tiled
    panes:
      - cmd: "ssh $( knife search 'hostname:image-1 AND chef_environment:production' | grep IP | sed 's/IP: *//' )"
      - cmd: "ssh $( knife search 'hostname:image-2 AND chef_environment:production' | grep IP | sed 's/IP: *//' )"
      - cmd: "ssh $( knife search 'hostname:image-3 AND chef_environment:production' | grep IP | sed 's/IP: *//' )"
      - cmd: "ssh $( knife search 'hostname:image-4 AND chef_environment:production' | grep IP | sed 's/IP: *//' )"
      - cmd: "ssh $( knife search 'hostname:image-5 AND chef_environment:production' | grep IP | sed 's/IP: *//' )"
      - cmd: "ssh $( knife search 'hostname:image-6 AND chef_environment:production' | grep IP | sed 's/IP: *//' )"
